---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: nix-chart-latest
spec:
  imageRepositoryRef:
    name: nix-chart
  policy:
    semver:
      range: ">=0.0.1000-0 <1.0.0000"
