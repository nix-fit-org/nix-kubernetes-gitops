---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: platform
spec:
  valuesFrom:
    - kind: ConfigMap
      name: platform-values
      valuesKey: values.yaml
    - kind: Secret
      name: platform-secrets-values
      valuesKey: secrets.values.yaml
