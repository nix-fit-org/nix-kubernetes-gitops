---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gateway
spec:
  valuesFrom:
    - kind: ConfigMap
      name: gateway-values
      valuesKey: values.yaml
    - kind: Secret
      name: gateway-secrets-values
      valuesKey: secrets.values.yaml
